import Link from 'next/link';

const linkStyle = {
    marginRight: 15,
    border: '1px solid #DDD',
    borderColor: 'blue'
};

const Footer = () => (
    <div>
        <p>Created by Project Recipe</p>
    </div>
);

export default Footer;